; ============================================================================
;  config.example.ini — TEMPLATE (safe to commit to GitHub)
;
;  To run the app locally:
;    1. copy this file to  config.ini   (config.ini is git-ignored)
;    2. supabase.com -> your project -> Project Settings -> API
;    3. paste your Project URL and anon public key into config.ini
;
;  You can also provide these as environment variables instead:
;    SUPABASE_URL, SUPABASE_ANON_KEY, PORT
; ============================================================================

SUPABASE_URL = https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY = PASTE_YOUR_SUPABASE_ANON_PUBLIC_KEY_HERE
PORT = 3000
