# Superseded — 3D model direction cancelled

The 3D/GLB arena-model plan this file used to describe has been **cancelled**.

The arena viewer is now built with **pure HTML + SVG + CSS** in an indie
Undertale style — see `js/arenaViewer.js` and the `.arena-*` styles in
`style.css`. No Three.js, no GLB files, no WebGL. Nothing belongs in this
folder; you can delete it.
